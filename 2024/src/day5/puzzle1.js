export function solve(input){
  const [first_section, second_section] = input
    .trim()
    .split(/\r?\n\r?\n/)
    .map(part => part.split(/\r?\n/));


  console.log("first_section :", first_section);
  console.log("second_section :", second_section);

  const counter = 0;



  return counter;
}
