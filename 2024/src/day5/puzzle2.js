export function solve(input){
  const lines = input.trim().split('\n');
  const letters = lines.map(line => line.trim().split(''));
  //console.log(lines);
  //console.log(letters);
  const counter = 0;



  return counter;
}
